// n8n's own shareable config — the same rule set their verification scanner
// runs, so problems surface here rather than three releases later.
import { config } from '@n8n/node-cli/eslint';

export default config;
